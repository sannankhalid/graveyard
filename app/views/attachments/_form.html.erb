<%= form_with(model: attachment, local: true) do |form| %>
  <% if attachment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(attachment.errors.count, "error") %> prohibited this attachment from being saved:</h2>

      <ul>
      <% attachment.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :created_by_id %>
    <%= form.number_field :created_by_id, id: :attachment_created_by_id %>
  </div>

  <div class="field">
    <%= form.label :updated_by_id %>
    <%= form.number_field :updated_by_id, id: :attachment_updated_by_id %>
  </div>

  <div class="field">
    <%= form.label :attachable_id %>
    <%= form.number_field :attachable_id, id: :attachment_attachable_id %>
  </div>

  <div class="field">
    <%= form.label :attachable_type %>
    <%= form.text_field :attachable_type, id: :attachment_attachable_type %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
